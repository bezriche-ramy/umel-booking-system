# --- Google Places (avis) ---
GOOGLE_PLACES_API_KEY=
GOOGLE_PLACE_ID=ChIJ3wTGU2ch-kcRP6Cd9pQrkG0

# --- Base de données PostgreSQL (Neon / Supabase / Vercel Postgres) ---
# URL poolée pour l'application, URL directe pour les migrations Prisma
DATABASE_URL="postgresql://user:password@host/db?sslmode=require&pgbouncer=true"
DIRECT_URL="postgresql://user:password@host/db?sslmode=require"

# --- Session admin (/admin) : 32 caractères aléatoires minimum (openssl rand -hex 32) ---
SESSION_SECRET=

# --- Stripe (empreinte bancaire 20 €) ---
STRIPE_SECRET_KEY=sk_live_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...

# --- E-mails (Resend) : domaine umelcouture.com à vérifier dans Resend ---
RESEND_API_KEY=re_...
EMAIL_FROM="Umel Couture <contact@umelcouture.com>"
EMAIL_REPLY_TO=contact@umelcouture.com

# --- Cron Vercel (relances J-3 / retouches) ---
CRON_SECRET=
